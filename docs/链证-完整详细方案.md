# 《链证》智能合约可验证安全审计平台 — 完整详细方案

> 赛题：2026 第八届全球校园人工智能算法精英大赛 · 算法创新赛 · 赛题 2：AI+软件创新
> 周期：4 周（**7 天/周**，共 28 天 × 3 人 ≈ **84 人天**）
> 资源：3 人（均能写代码）｜4090 + A100｜AI 每日可自主跑批
> 定稿：2026-09-20

---

# 第 0 章 项目概览

| 项 | 内容 |
|---|---|
| 作品名称 | 《链证：智能合约可验证安全审计平台》（17 字） |
| 一句话 | 上传 Solidity 合约 → 输出漏洞报告 + **形式化证明** + 修复建议 |
| 产品类型 | AI 辅助设计/分析工具 · 在线服务 |
| 目标用户 | 高校区块链课程与竞赛团队 · 联盟链/产业区块链开发者 |
| 核心创新 | LLM 自动生成形式化规约，交给求解器证明 |
| 预期分数 | 80-90 |

---

# 第 1 章 产品定义

## 1.1 主流程

```
上传合约 → 编译 → 语义理解 → 规约生成 → 符号执行 → 求解器验证
        → 反例解释 + 补丁生成 → 报告 → 应用补丁 → 重跑确认
```

## 1.2 六个模块

| # | 模块 | 展开维度 | 一句话卖点 |
|---|---|---|---|
| **M1** | 项目空间与审计历史 | 时间 + 数量 + 协作 | "这次改动，修好了哪些洞、又新引入哪些"（审计 diff） |
| **M2** | 审计结果与证明视图 ⭐ | 核心交互 | "每条漏洞点开就是**可展开的证明**和**反例轨迹**" |
| **M3** | 规约库 | 维护 | "审计标准本身公开可查，每条规约标 SWC 出处" |
| **M4** | 报告导出与分享 | 传递 | "每个结论都带证明链接" |
| **M5** | 批量审计 + CLI/CI | 数量 + 工程化 | "提交代码自动审计，PR 里直接评论漏洞" |
| **M6** | 漏洞知识库 | 场景 | "面向高校区块链课程的原理+案例+复现+修复" |

## 1.3 范围边界（写死）

**覆盖 7 类漏洞**：重入 · 整数溢出/下溢 · 访问控制缺陷 · 未检查的外部调用返回值 · tx.origin 鉴权 · 时间戳依赖 · Gas 限制 DoS

**明确不覆盖**：跨合约复杂不变式 · 代币经济/经济模型漏洞 · 链下组件 · Gas 优化 · 升级代理模式完整验证 · 非 Solidity 语言

## 1.4 场景叙事（政策友好，必须）

| ❌ 不写 | ✅ 改写 |
|---|---|
| Web3 项目方、交易所 | **高校区块链课程与竞赛团队** |
| | **联盟链/产业区块链开发者**（供应链金融、存证、数字人民币相关） |

---

# 第 2 章 创新点

## 2.1 核心主张

> **用 LLM 自动生成形式化规约，交给求解器证明——把形式化验证从"专家手写规约"变成"自动生成 + 机器证明"。**

## 2.2 打败的对手

| 现有工具 | 问题 |
|---|---|
| 纯启发式（Slither / Mythril） | 快，但**误报高** |
| 纯形式化（Certora 等） | 结论硬，但**要专家一笔一划写规约**，落不了地 |

## 2.3 关键判断

> **形式化验证几十年落不了地，瓶颈从来不是求解器，是"谁来写规约"。**

## 2.4 诚实定位（答辩不能说错）

- "反例引导的精化""LLM+求解器"**已有先例**（CEGIS 2006、Houdini 2001、PropertyGPT 等）
- **我们的贡献是应用 + 闭环 + 信号设计，不是方法首创**
- 按评分表语言：**"在现有技术上有显著改进，形成独特解决方案"**，锚定 **12-16 分**，不规划 18+

---

# 第 3 章 技术架构

## 3.1 整体架构

```
┌─────────────── 产品层（C） ───────────────┐
│  Web 前端  │  后端 API + 任务队列  │  部署  │
└───────────────┬──────────────────────────┘
                │ schemas/analysis_result.json  ← 唯一契约
┌───────────────┴──────────────────────────┐
│  引擎层（A）         │   AI 层（B）        │
│  编译 / 符号执行      │   LLM 接入          │
│  求解器 / 反例解析    │   规约生成 + 闭环    │
│  规则库 / 路径裁剪    │   解释 / 补丁生成    │
└──────────────────────┴───────────────────┘
        ↑ schemas/path_summary.json（A → B 单向）
```

## 3.2 数据契约 ①：`schemas/analysis_result.json`

```json
{
  "contract": { "name": "str", "hash": "str", "compiler": "str" },
  "analysis": {
    "status": "done | partial | timeout",
    "coverage": { "proved": 6, "total": 10 },
    "duration_ms": 1234
  },
  "findings": [{
    "id": "F1",
    "swc": "SWC-107",
    "type": "reentrancy",
    "severity": "high | medium | low | info",
    "location": { "file": "str", "line": 42, "function": "withdraw" },
    "evidence": {
      "kind": "formal | counterexample | pattern",
      "smt_status": "sat | unsat",
      "counterexample": [{ "step": 1, "state": {} }],
      "proof_ref": "str"
    },
    "explanation": "str",
    "patch": { "diff": "str", "verified": true }
  }],
  "unverified": [{ "reason": "timeout", "scope": "function foo" }]
}
```

> `explanation` 与 `patch` 由 **B** 填充；其余由 **A** 填充；**C** 只读渲染。

## 3.3 数据契约 ②：`schemas/path_summary.json`

```json
{
  "functions": [{
    "name": "str", "visibility": "str",
    "state_vars_written": ["str"],
    "external_calls": ["str"],
    "paths_explored": 12,
    "suspicious_score": 0.8
  }],
  "state_vars": [{ "name": "str", "type": "str", "invariants_hint": "str" }],
  "solver_feedback": [{ "property": "str", "status": "sat", "counterexample_brief": "str" }]
}
```

**这两个 schema 就是三人之间唯一的强接口。**

---

# 第 4 章 AI 算法设计

## 4.1 AI 的四个位置

| # | 位置 | 输入 → 输出 | 算法 | 没有它会怎样 |
|---|---|---|---|---|
| 1 | 代码语义理解 | Solidity → 合约意图、状态变量、外部调用、权限边界 | 代码大模型（长上下文） | 无法把代码与漏洞模式对应 |
| **2** | **规约生成** ⭐ | 代码 + 语义 → 形式化性质 | 代码大模型 + **求解器反馈闭环** | 退化成"又一个符号执行工具" |
| 3 | 路径排序 | 路径摘要 → 可疑度 | 轻量分类器 / 交叉编码器 | 路径爆炸跑不完 |
| 4 | 反例解释 + 补丁 | 反例轨迹 → 人话 + 补丁 | 代码大模型（漏洞类型条件约束） | 用户拿到 SAT/UNSAT 也不知怎么办 |

## 4.2 非 AI 的确定性内核（正确性来源）

| 组件 | 工具 |
|---|---|
| 符号执行 | Mythril / Manticore / Slither |
| 形式化验证 | **Z3 / CVC5**（SMT-LIB） |
| 漏洞模式库 | 对齐 SWC Registry |

## 4.3 四个"为什么"（「AI 算法设计科学」的答辩答案）

| 问题 | 答案 |
|---|---|
| 为什么 LLM 生成规约，不人工写？ | 形式化验证的瓶颈是规约，不是求解 |
| 为什么求解器判定，不让 LLM 判断？ | **求解器不可幻觉**；SAT/UNSAT 是证明 |
| 为什么用 rejection sampling 不用 RL？ | **有确定性奖励信号**（验证通过/不通过），无需学奖励模型；RL 不稳定、样本效率低 |
| 为什么做路径排序？ | 路径爆炸是符号执行的固有难题 |

## 4.4 训练方案

| 项 | 内容 |
|---|---|
| 训练目标 | 规约生成（主）+ 补丁生成（次） |
| 基座 | 代码大模型 7B 级（QLoRA） |
| 数据 | **rejection sampling 自动生成**：候选规约 → 求解器筛 → 保留通过的 |
| 数据量 | 3,000-10,000 条（质量 > 数量） |
| 训练成本 | **4090：3-6 小时**；A100：1-2.5 小时 |
| 关键前提 | **先测 API baseline**；若已达标则不训 |
| 止损判据 | 候选通过率 < 5% → 放弃训练，退回"API + 提示工程 + 人工确认" |
| 对比实验 | **微调 vs API** → 出一张对比表 |

---

# 第 5 章 数据方案

## 5.1 四类数据源

| 类 | 内容 | 用途 |
|---|---|---|
| **A 漏洞标注** | SmartBugs Curated(143) · SmartBugs wild(4.7万+) · **SolidiFI(注入漏洞，真值已知)** · DAppSCAN(2.3万+) | 检测评测 |
| **B 真实攻击** | **DeFiHackLabs** · Damn Vulnerable DeFi · Ethernaut | 证明能抓真洞 |
| **C 规约样本** ⭐ | **Foundry invariant 测试** · **Certora CVL 规约** · Scribble 注解 · solc-verify/halmos 示例 | **训练规约生成**（自己爬取整理） |
| **D 分类本体** | **SWC Registry** · Solodit · Code4rena | 规则库与本体 |

## 5.2 关键动作

| 动作 | 说明 |
|---|---|
| **误报真值** | 必须用**干净合约**当负样本——先确认 SolidiFI 是否给注入前原版；不给就从 SmartBugs 挑 |
| 规约样本整理 | 从开源仓库爬取 (合约, 规约) 对，**这是数据贡献** |
| 合规纪律 | **只给运行时拉取脚本，绝不打包进安装包/镜像**；逐条标注引用出处；先核对许可证 |

---

# 第 6 章 指标与实验

## 6.1 指标表（全部自动可测）

| 指标 | 测法 | 真值来源 |
|---|---|---|
| 漏洞检出率 / 漏报率 | SolidiFI 注入漏洞抓到多少 | ✅ 注入即知真值 |
| **误报率** | **干净合约**跑一遍 | ⚠️ 需自备负样本 |
| 规约有效率 | 生成的规约抓住多少已知漏洞 | ✅ 求解器自动判 |
| 形式化覆盖率 | 多少函数/性质被证明 | ✅ 自动 |
| 工具对比 | Slither / Mythril 在同样样本上的表现 | ✅ 自动 |
| 补丁有效率 | 打补丁后重跑，漏洞是否消失 | ✅ 自动 |

## 6.2 四组实验

| # | 实验 | 对照 |
|---|---|---|
| E1 | **AI 必要性** | 只用规则/工具 vs 加规约生成 |
| E2 | **微调 vs API** | QLoRA 模型 vs API baseline |
| E3 | **工具对比** | 本产品 vs Slither vs Mythril（检出/误报） |
| E4 | **补丁有效性** | 应用补丁前后重跑 |

## 6.3 复现

- 评测脚本随仓库发布
- 数据集只给拉取脚本
- **评审可自行跑：给定合约 → 复现同一结果**

---

# 第 7 章 三棵树分工

## 7.1 目录与归属

| 树 | 归属 | 内容 |
|---|---|---|
| `engine/` | **A** | compiler / symbolic / solver / counterexample / rules / paths / cli |
| `ai/` | **B** | llm / spec / explain / patch / dataset / pipeline |
| `server/` `web/` `deploy/` | **C** | api / queue / storage / auth ／ core / modules(M1-M6) / assets ／ 部署 |
| `schemas/` | **共享** | 两个 JSON 契约 |

## 7.2 独立可运行产物（独立性的硬标准）

| 人 | 产物 |
|---|---|
| A | CLI：`analyze contract.sol → result.json` |
| B | CLI：`specgen contract.sol → specs.json` |
| C | Web：喂 mock JSON，全流程可点 |

## 7.3 AI 上下文边界（少打扰的关键）

| 原则 | 说明 |
|---|---|
| 每人的 AI 会话只需装 | ① 自己那棵树 ② `schemas/` |
| 不需要读别人的代码 | **契约是 JSON 而不是函数调用** → AI 不必跨树理解 |
| 提示词里不出现别人目录 | 上下文独立，互不污染 |
| 前端是 AI 加速最猛的部分 | C 活最多，但组件/CRUD/样式正是 AI 最擅长 |

## 7.4 禁止清单

| ❌ 禁止 | 后果 |
|---|---|
| 改 `schemas/` 的字段名/类型 | 另两人代码当场崩 |
| 在别人目录加文件 | 合并冲突 + 上下文污染 |
| 复制别人代码改 | 双份维护 |
| 口头约定接口 | AI 无法据口头约定生成正确代码 |
| 站会超过 15 分钟 | 时间被会议吃掉 |

---

# 第 8 章 四周排期（7 天/周 · 28 天）

## 8.1 每日节奏（利用 AI 自主跑批）

| 时间 | 动作 |
|---|---|
| 09:00-09:15 | 站会：昨日产出 / 今日计划 / **卡点** |
| 09:15-18:00 | 白天：**设计、调试、集成、实验判断**（AI 加速编码） |
| **18:00** | **提交夜间批处理任务** |
| 次日 09:00 | 看批处理结果 |

## 8.2 夜间批处理任务池（AI 自主跑，无需人盯）

| 任务 | 说明 |
|---|---|
| 合约批量分析 | 100-500 个合约一次跑完 |
| **rejection sampling** | 生成候选规约 → 求解器筛 |
| QLoRA 训练 | 3-6 小时 |
| 指标评测跑批 | 全量指标 |
| 回归测试 | 全量单测 |
| 数据集预处理 | 清洗、切分、格式转换 |

## 8.3 逐周计划

### W1（Day 1-7）：打通与 baseline

| 日 | A（engine） | B（ai） | C（product） |
|---|---|---|---|
| **D1** | **三人一起冻结两个 schema** + 建仓库 + 分支策略 | | |
| D2 | solc 编译 + 符号执行跑通；**交付 PathSummary v0（内容可假）** | 下载全部数据集 | 前端基础设施 + 组件库 |
| D3 | Z3 调用 + SMT-LIB | **API baseline 开始**（100 个注入漏洞） | **假集成**：mock 串全流程 |
| D4 | 反例解析格式定稿 | 规约生成 v0 | M2 结果页骨架 + 源码高亮 |
| D5 | 漏洞模式库 + 规则 | **拿到 baseline 数字 X%** | M2 证明展开 |
| D6 | 超时管理 | 解释生成 v0 | M2 反例轨迹可视化 |
| **D7** | 🔴 **真集成死线：上传合约 → 出报告 + 一个可展开的证明** | | |

### W2（Day 8-14）：闭环与模块

| 人 | 目标 |
|---|---|
| A | 路径裁剪 + 更多漏洞类型 + 覆盖率统计 |
| B | **规约生成闭环**（生成→验证→反馈）+ 数据流水线 + QLoRA 第一版 |
| C | M1 项目空间（审计 diff）+ M3 规约库 + 主流程健壮性（超时/部分成功/未覆盖声明） |
| **D14** | **中期集成** + 第一次完整指标 |

### W3（Day 15-21）：指标与上线

| 人 | 目标 |
|---|---|
| A | 稳定性 + 性能（并发/耗时） |
| B | **完整指标跑齐**（检出/漏报/误报/覆盖率/工具对比）+ 微调 vs API 对比 |
| C | M4 报告导出 + M5 批量与 CLI/CI + **上线** + **招募 30 位用户** |
| **D21** | **上线节点** + 用户试点启动 |

### W4（Day 22-28）：材料与冻结

| 人 | 目标 |
|---|---|
| A | 引擎部署文档 + 方案「代码实现」章节 |
| B | 数据整理成表 + 方案「核心算法说明」+ 测试报告数据部分 |
| C | M6 + **演示视频** + 材料七件套汇总 + **匿名化自查** |
| **D28** | **代码冻结 + 材料冻结** |

---

# 第 9 章 资源与工具

| 项 | 内容 |
|---|---|
| 硬件 | 4090（训练/推理）、A100（大批量 rejection sampling） |
| 工具链 | solc · Mythril · Manticore · Slither · Z3 · CVC5 |
| AI | 代码大模型 API + 本地部署双通道 |
| 训练 | QLoRA（peft/trl）｜4090 够 |
| 部署 | Docker + 云服务器 + 在线地址 |
| **A100 用途** | ① rejection sampling ② QLoRA 微调 ③ 微调 vs API 对比 |

**注意**：本地无 GPU（torch 是 CPU 版），GPU 在远程服务器（SSH）；**C 盘仅剩 2.1GB，所有下载与环境必须落 D 盘**。

---

# 第 10 章 风险与对策

| # | 风险 | 严重度 | 对策 |
|---|---|---|---|
| 1 | **符号执行路径爆炸/超时** | 🔴 最高 | 限制路径深度 + LLM 排序裁剪；**Day7 必须验证** |
| 2 | 误报缺真值 | 🟡 | 备好干净合约当负样本；确认 SolidiFI 是否给原版 |
| 3 | 规约强度调不好 | 🟡 | 不追求最优；保留历史最优 |
| 4 | 政策叙事 | 🟡 | 换联盟链/高校场景 |
| 5 | 数据许可 | 🟡 | 只给运行时拉取脚本 |
| 6 | **匿名化** | 🚨 **一票否决** | 材料三人交叉自查 |
| 7 | 三人负载不均（B 最重） | 🟡 | C 用 AI 猛攻前端；必要时砍 M6 |
| 8 | 材料留到 W4 | 🟡 | **从 W2 起每人交自己章节初稿** |

---

# 第 11 章 材料清单（七件套）

| # | 材料 | 要求 | 负责 |
|---|---|---|---|
| 1 | 作品名称 | ≤20 字 | C |
| 2 | 作品简介 | ≤300 字，问题-方案-效果-应用 | C |
| 3 | 作品方案 PDF | 严格照六节大纲，**图表规范**，≤10M | 三人各写章节，C 汇总 |
| 4 | 演示视频 | MP4，**3-5 分钟**，≤300MB | C |
| 5 | 答辩 PPT → PDF | — | C |
| 6 | 佐证材料（合并 1 PDF） | 软著受理 + 用户反馈 + 测试数据 | C |
| 7 | 网盘链接 | 命名：团队编号-赛题名-作品名-XX | C |

**大纲对照（必须逐节写）**：
- (一) 产品概述与定位：**加"对 AI 软件领域发展的意义"**
- (二) 技术架构与创新：架构图 + 选型依据 + **对比同类产品**
- (三) 功能设计与实现：核心功能 + **辅助功能与 AI 的协同** + **扩展性设计**
- (四) 开发测试与部署：流程 + 代码 + **AI 专项测试 + 改进闭环** + 部署
- (五) 应用价值与展望：场景 + 市场 + 成果 + **不足（算法精度/兼容性/并发/场景覆盖四维）**
- (六) 附录：设计资料 + 核心代码 + 测试数据 + 参考文献

---

# 第 12 章 预期分数

| 维度 | 预期 | 依据 |
|---|---|---|
| 创新性 | **12-16** | 显著改进、独特解决方案 |
| 技术实现 | **15-18** | AI 在关键路径 + 确定性内核兜底 + 消融实验 |
| 功能完整性 | **18-19** | 六模块 + 主流程无缺口 |
| 方案完整性 | **19** | 指标全自动可测，数据充足 |
| 应用价值 | **11-13** | 高校课程/联盟链场景，可量化 |
| 总结与展望 | **5** | 范围边界 + 四维不足分析 |
| **合计** | **80-90** | |

---

# 第 13 章 开工前立即要做的 6 件事

| # | 事项 | 时间 |
|---|---|---|
| 1 | **下载官方作品方案模板**（网盘 pwd=2026） | **立即** |
| 2 | **提交软著申请**（拿受理通知书） | **Day 1** |
| 3 | 冻结两个 JSON schema | **Day 1 上午** |
| 4 | 从 GitHub 拉全部数据集 | Day 1-2 |
| 5 | **API baseline**：100 个注入漏洞 → 抓到 X% | Day 2-5 |
| 6 | 确认 SolidiFI 是否提供注入前原版 | Day 1 |
