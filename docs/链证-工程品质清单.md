# 《链证》工程品质清单（推出级标准）

> 配套：`链证-完整方案.md` · `链证-分工与集成手册.md`
> **本清单的项目一项都不能省。** 范围可以窄（覆盖 7 类漏洞），品质不能低。
> 每项都给出**可验证的验收标准**——做不到就是没做到。

---

# 第 1 类 安全（最高优先级）

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| S1 | **分析在沙箱容器内执行** | 上传恶意合约（死循环、超内存、尝试读写宿主文件）→ 容器被限额终止，宿主无影响 | A | D4 |
| S2 | **沙箱无网络出口** | 容器内 `curl` 外网失败；DNS 不可解析 | A | D4 |
| S3 | **沙箱文件系统只读 + 只挂载必要目录** | 容器内写系统目录失败 | A | D4 |
| S4 | **资源限额**（CPU / 内存 / 磁盘 / 时间） | 超限任务被终止并返回明确错误，不拖垮宿主 | A | D4 |
| S5 | **每次分析用独立容器，跑完销毁** | 连续分析 100 个合约，无残留容器/临时文件 | A | W2 |
| S6 | **上传校验**（类型 / 大小 / 内容） | 非 `.sol` 文件、超大文件、二进制文件被拒 | C | D5 |
| S7 | **接口速率限制** | 单 IP 超频请求返回 429 | C | D5 |
| S8 | **密钥与凭据不落库、不进仓库** | `.env.example` 在仓库，真实 `.env` 在 `.gitignore`；`git log -p` 搜不到密钥 | C | W3 |
| S9 | **依赖与许可证审计** | 生成全部依赖的许可证清单；无 GPL/AGPL 污染（或已明确标注动态调用） | C | W4 |
| S10 | **依赖漏洞扫描** | 扫描报告无高危未处理项 | C | W4 |
| S11 | **数据来源合规** | 数据集**只给运行时拉取脚本**，不进镜像/安装包；逐条标注引用出处 | B | W4 |
| S12 | **隐私** | 用户上传的合约默认私有；不用于训练；提供删除入口 | C | W3 |

---

# 第 2 类 可靠性

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| R1 | **全链路错误处理** | 编译失败 / 求解超时 / LLM 失败 / 磁盘满 —— 每种都有明确提示，不白屏不卡死 | A+C | W2 |
| R2 | **超时降级为部分结果** | 复杂合约超时时返回 `status: partial` + 已得结论 + 未覆盖声明 | A | D6 |
| R3 | **任务幂等** | 同一合约重复提交不产生重复记录；相同 hash 复用缓存 | A+C | W2 |
| R4 | **失败可重试** | 前端提供重试；后端任务失败自动重试 N 次后标记失败 | C | W2 |
| R5 | **队列积压可见** | 队列深度、等待时长可见；超阈值告警 | C | W3 |
| R6 | **优雅关闭** | 服务重启时运行中任务不丢（落库或重新入队） | C | W3 |
| R7 | **数据持久化与备份** | 数据卷持久化；给出备份与恢复步骤并实测一次 | C | W3 |
| R8 | **降级开关** | LLM 不可用时，可退回"仅规则+符号执行"模式（产品不瘫痪） | B+C | W3 |

---

# 第 3 类 可观测性

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| O1 | **结构化日志** | JSON 格式；含请求 ID、合约 hash、耗时、结果状态 | A+C | W2 |
| O2 | **日志轮转** | 磁盘不会被打满 | C | W3 |
| O3 | **关键指标** | 分析耗时 P50/P95、成功率、超时率、LLM 轮次、求解器耗时、队列深度 | A | W2 |
| O4 | **指标面板** | 一页能看到上述全部指标的真实数据 | C | W3 |
| O5 | **健康检查** | `/healthz` 返回各依赖状态；编排系统可据此判断 | C | W3 |
| O6 | **失败告警** | 连续失败 / 队列积压 / 磁盘不足 触发告警（邮件或 Webhook） | C | W4 |
| O7 | **每次分析的诊断信息** | 报告里含：模型版本、规约版本、工具版本、耗时分解 | A+B | W3 |

---

# 第 4 类 可维护性

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| M1 | **配置管理** | 全部配置走环境变量；`.env.example` 完整；缺关键配置启动即报错并指明缺哪个 | C | W3 |
| M2 | **启动自检** | 启动时校验：solc 可用、Z3 可用、沙箱可用、LLM 可达、磁盘余量 | A+C | W3 |
| M3 | **代码规范** | 统一 linter + formatter；CI 检查；无未使用代码 | 三人 | W2 |
| M4 | **类型标注** | Python 全量类型标注；前端启用类型检查 | 三人 | 全程 |
| M5 | **单元测试** | 引擎/AI/后端核心函数有单测；覆盖率报告 | 三人 | W3 |
| M6 | **集成测试** | 端到端测试脚本：上传 → 分析 → 报告，一键跑 | C | W3 |
| M7 | **回归测试集** | 固定 50 个合约 + 期望结果，每次改动后跑 | C | W3 |
| M8 | **契约测试** | 校验产出物符合 JSON schema（三方各跑） | 三人 | W2 |
| M9 | **版本与发布** | 语义化版本；CHANGELOG；镜像带 tag；**实测一次回滚** | C | W4 |
| M10 | **依赖锁定** | 锁文件提交；新环境可复现安装 | 三人 | D2 |
| M11 | **文档：API** | OpenAPI 自动生成 + 请求示例；评审可自助调用 | C | W3 |
| M12 | **文档：部署与运维** | 部署指南 + 运维手册 + 故障排查；外人照做能起 | C | W4 |
| M13 | **文档：用户指南** | 新用户 5 分钟上手；含常见问题 | C | W4 |
| M14 | **文档：架构与设计** | 架构图 + 模块设计 + 数据流 + 两个 schema 说明 | 三人 | W4 |

---

# 第 5 类 可部署性

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| D1 | **一键启动** | `docker compose up` 起全部服务；无需手工步骤 | C | W3 |
| D2 | **环境隔离** | dev / prod 配置分离；dev 可用 mock 引擎 | C | W3 |
| D3 | **镜像精简** | 多阶段构建；镜像体积合理；无构建残留 | C | W3 |
| D4 | **公网可访问 + HTTPS** | 评审在任意网络可打开；证书有效 | C | W3 |
| D5 | **离线部署支持** | 内网/涉密场景可本地部署（含无外网时的降级说明） | C | W4 |
| D6 | **数据卷与初始化** | 首次启动自动建表/建索引；空库可正常使用 | C | W3 |
| D7 | **资源声明** | 明确最低配置（CPU/内存/磁盘）与推荐配置 | C | W4 |

---

# 第 6 类 可用性（用户体验）

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| U1 | **首次使用引导** | 新用户进来有示例合约一键体验，不需自己准备文件 | C | W2 |
| U2 | **空态设计** | 无项目 / 无结果 / 无漏洞 —— 每种空态都有引导文案 | C | W2 |
| U3 | **加载态与分析进度** | 长任务有进度（编译中 → 生成规约中 → 验证中），不是干等 | C | W2 |
| U4 | **超时与失败提示** | 明确说明"卡在哪一步、为什么、可以怎么办" | C | W3 |
| U5 | **未覆盖声明** | 明确标出"这部分我们没验证"——**这是本产品的诚实基因** | C | W2 |
| U6 | **源码定位** | 每处漏洞点击直达源码行并高亮 | C | W2 |
| U7 | **证明可展开** | 每条结论可展开看到证明/反例细节，不是黑盒结论 | C | D6 |
| U8 | **响应式** | 手机可访问核心流程 | C | W3 |
| U9 | **键盘可达 + 语义化标签** | Tab 能走完主流程；表单有 label | C | W3 |
| U10 | **错误文案规范** | 不说"出错了"，说清"什么错、为什么、怎么办" | C | W3 |

---

# 第 7 类 合规

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| C1 | **遵守赛题材料规范** | 名称 ≤20 字；简介 ≤300 字；方案 PDF ≤10M；视频 MP4 3-5 分钟 ≤300MB；PPT 转 PDF；佐证合并 1 PDF | C | W4 |
| C2 | 🚨 **匿名化** | **材料中无校名、校徽、指导教师、团队编号以外的身份信息** —— 三人交叉自查两轮 | 三人 | W4 |
| C3 | **自主知识产权** | 软著已受理；自研部分清晰（引擎 + 规约闭环 + 产品） | C | W1 提交 |
| C4 | **引用规范** | 论文/数据集/国标逐条标注出处 | B | W4 |
| C5 | **数据合法授权** | 公开数据集仅运行时拉取；核对每个数据集许可证 | B | W2 |
| C6 | **场景表述合规** | 不出现"Web3 交易/交易所"等敏感表述；改为高校课程 + 联盟链 | 三人 | W4 |
| C7 | **不提供法律/投资意见** | 报告含免责声明：本工具为技术辅助，结论需人工复核 | C | W3 |

---

# 第 8 类 发布管理

| # | 要求 | 验收标准 | 负责 | 阶段 |
|---|---|---|---|---|
| P1 | **版本号规范** | 语义化版本（`v1.0.0`）；镜像 tag 与代码 tag 一致 | C | W4 |
| P2 | **CHANGELOG** | 记录每个版本的变更 | C | W4 |
| P3 | **可回滚** | 保留上一个可用镜像；**实测一次回滚** | C | W4 |
| P4 | **发布清单** | 发布前跑：全部测试 + 回归集 + 冒烟测试 | C | W4 |
| P5 | **演示环境稳定** | 提前准备本地兜底 demo；**视频提前录好**（防现场故障） | C | W4 |
| P6 | **代码冻结** | D28 冻结；之后只修阻塞性 bug | 三人 | D28 |

---

# 第 9 类 交付物对照（写进方案 (六) 附录）

| 交付物 | 内容 | 负责 |
|---|---|---|
| 软件架构文档 | 架构图、模块设计、数据流、两个 schema | A+B |
| 核心代码说明 | 规约生成闭环、反例结构化、沙箱封装、缓存策略 | A+B |
| 测试报告 | 四组实验（AI 必要性 / 微调对比 / 工具对比 / 补丁有效性）+ 原始数据 | B |
| 性能报告 | 单合约耗时、批量耗时、并发能力、资源占用 | A+C |
| 部署与运维文档 | 一键部署、配置说明、故障排查 | C |
| 用户指南 | 上手流程、常见问题 | C |
| API 文档 | OpenAPI + 示例 | C |
| 许可清单 | 全部依赖许可证 + 数据集引用出处 | C+B |

---

# 附：验收自查表（W4 用，逐项打勾）

## 安全
- [ ] S1-S5 沙箱五项全部实测通过
- [ ] S6-S7 上传校验与限流实测通过
- [ ] S8 密钥扫描无泄漏
- [ ] S9-S10 许可审计 + 漏洞扫描完成
- [ ] S11 数据集不进包
- [ ] S12 隐私声明与删除入口

## 可靠性
- [ ] R1-R8 全部实测（含故意触发失败路径）

## 可观测性
- [ ] O1-O7 完成，面板能看到真实数据

## 可维护性
- [ ] M1-M14 完成，含 API/运维/用户/架构四类文档
- [ ] M7 回归集可一键跑
- [ ] M9 实测过回滚

## 可部署性
- [ ] D1 一键起
- [ ] D4 公网 HTTPS 可访问
- [ ] D5 离线部署说明

## 可用性
- [ ] U1-U10 完成，特别是 **U5 未覆盖声明** 与 **U7 证明可展开**

## 合规
- [ ] 🚨 **C2 匿名化两轮交叉自查完成**
- [ ] C1 材料七件套格式全部达标
- [ ] C3 软著受理通知书在手

## 发布
- [ ] P1-P6 完成，含实测回滚与提前录制的视频
