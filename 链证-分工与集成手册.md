# 《链证》分工与集成手册（推出级）

> 配套：`链证-完整方案.md` · `链证-工程品质清单.md`
> 本文只讲三件事：**每人具体做什么** · **怎么集成** · **纪律**

---

# 第 0 章 一页看懂

```
                ┌── schemas/ ──┐   ← 唯一共享面（两个 JSON）
                │              │
 A：engine/ ────┼──────────────┼──── B：ai/
                │              │
                └──────┬───────┘
                       │
              C：server/ web/ deploy/
```

| | **A** | **B** | **C** |
|---|---|---|---|
| **拥有** | `engine/` | `ai/` | `server/` `web/` `deploy/` |
| **一句话** | 合约进，证据出 | 让 AI 把证据补全 | 把证据变成产品 |
| **独立产物** | CLI：`analyze x.sol → result.json` | CLI：`specgen x.sol → specs.json` | Web：喂 mock JSON 全流程可点 |
| **等谁** | 谁也不等 | 只等 A 的 `PathSummary` | **谁也不等** |
| **人天** | ≈ 36 | ≈ 38 | ≈ 40 |

**两条依赖（只有两条）**：
1. `A 的 PathSummary → B 的规约生成`
2. `A + B 的真结果 → C 的界面`（C 先用 mock，不阻塞）

**四个集成节点**：Day 1 冻结 ｜ Day 3 假集成 ｜ **Day 7 真集成（死线）** ｜ 每周五

---

# 第 1 章 唯一契约

## 1.1 `schemas/path_summary.json`（A 产 → B 用）

```json
{
  "contract": { "name": "Token", "file": "Token.sol", "solc": "0.8.20" },
  "functions": [{
    "name": "withdraw",
    "visibility": "public",
    "state_vars_written": ["balances", "totalSupply"],
    "external_calls": ["msg.sender.call{value: amt}(\"\")"],
    "paths_explored": 12,
    "suspicious_score": 0.82,
    "notes": "外部调用后仍写状态变量"
  }],
  "state_vars": [
    { "name": "balances", "type": "mapping(address=>uint256)", "invariants_hint": "总量守恒" }
  ],
  "solver_feedback": [
    { "property": "balances[msg.sender] >= 0", "status": "unsat", "counterexample_brief": null }
  ]
}
```

## 1.2 `schemas/analysis_result.json`（最终产物）

```json
{
  "contract": { "name": "Token", "hash": "sha256:...", "compiler": "0.8.20" },
  "analysis": {
    "status": "done | partial | timeout | failed",
    "coverage": { "proved": 6, "total": 10 },
    "duration_ms": 4210,
    "model_version": "specgen-v1.2"
  },
  "findings": [{
    "id": "F1",
    "swc": "SWC-107",
    "type": "reentrancy",
    "severity": "high | medium | low | info",
    "location": { "file": "Token.sol", "line": 42, "function": "withdraw" },
    "evidence": {
      "kind": "formal | counterexample | pattern",
      "smt_status": "sat | unsat",
      "counterexample": [
        { "step": 1, "state": { "balances[attacker]": 100 } },
        { "step": 2, "state": { "reentrant_call": true } }
      ],
      "proof_ref": "proof/F1.smt2"
    },
    "explanation": "withdraw 在转账后才扣减余额，攻击者可在回调中再次调用，重复提现。",
    "patch": { "diff": "--- a/Token.sol\n+++ b/Token.sol\n@@ ...", "verified": true }
  }],
  "unverified": [{ "reason": "timeout", "scope": "function complexLoop" }]
}
```

## 1.3 字段归属（写死，谁都不许越界）

| 字段 | 谁填 |
|---|---|
| `contract` `analysis` `findings[].{id,swc,type,severity,location,evidence}` `unverified` | **A** |
| `analysis.model_version` | **B** |
| `findings[].explanation` `findings[].patch` | **B** |
| 全部字段 | **C 只读渲染** |

---

# 第 2 章 人 A 的任务清单（engine/）

## 2.1 目录

```
engine/
├── compiler/       多版本 solc 编译（0.4 / 0.5 / 0.6 / 0.7 / 0.8）
├── sandbox/        沙箱执行封装（容器隔离、资源限额、禁网）
├── symbolic/       符号执行（Mythril 主 / Manticore 备）
├── static/         静态分析初筛（Slither）
├── solver/         Z3 主 / CVC5 备 + SMT-LIB 生成
├── counterexample/ 反例解析 → 结构化轨迹
├── rules/          7 类漏洞模式库（对齐 SWC）
├── paths/          路径裁剪 + 超时降级
├── cli/            analyze 入口（--json / --batch / --format）
└── obs/            日志、指标、耗时埋点
```

## 2.2 任务

| # | 任务 | 输出 | **自测方法** | 截止 |
|---|---|---|---|---|
| A1 | **多版本编译封装** | AST + bytecode + 编译错误原文 + 版本自动探测 | 5 个不同版本合约各编译一次 | D3 |
| A2 | **沙箱执行封装** 🔴 | 容器内执行、限额、禁网、跑完销毁 | **故意跑死循环合约，验证能被限额杀掉** | D4 |
| A3 | 符号执行封装 | 路径列表 + 状态变量读写 | SolidiFI 样本跑通 | D3 |
| A4 | 静态分析初筛 | Slither 结果归一化 | 与 Slither 直跑结果一致 | D4 |
| A5 | Z3 封装 + CVC5 备选 | SAT/UNSAT + model | 手写 5 条已知性质验证 | D4 |
| A6 | **反例解析** | 结构化轨迹（符合 schema） | 结构对得上 | D5 |
| **A7** | **PathSummary 生成** | `path_summary.json` | mock 合约先验格式 | **D2（v0）** |
| A8 | 漏洞模式库 | 7 类规则 + SWC 映射 | 每类有正/负样本 | D5 |
| A9 | **路径裁剪 + 超时降级** | 部分结果而非失败 | 喂复杂合约，能出 partial | D6 |
| A10 | 覆盖率统计 | `coverage` 字段 | 数字对得上 | W2 |
| A11 | CLI（`--json` / `--batch` / `--format`） | `result.json` | 命令行跑出文件 | D6 |
| A12 | 接 B 的规约 | 合并进 result | 联调 | W2 |
| A13 | 接 C 的 API / 队列 | 引擎可被调度 | 联调 | W2 |
| A14 | **可观测性埋点** | 耗时/失败率/超时率指标 | 指标能被 C 拉取 | W2 |
| A15 | 引擎单元 + 集成测试 | 测试集 | 覆盖率报告 | W3 |
| A16 | 稳定性 + 性能（并发） | 并发/耗时数据 | 100 合约批量不断 | W3 |
| A17 | 部署文档（引擎部分）+ 方案「代码实现」章节 | 文档 | — | W4 |

## 2.3 A 的验收标准

> **① `analyze Token.sol -o result.json` 输出符合 schema 的结果，含真实 SAT/UNSAT 与可展开反例；② 故意上传恶意/死循环合约，沙箱能安全终止；③ 支持 0.4-0.8 主流版本编译。**

---

# 第 3 章 人 B 的任务清单（ai/）

## 3.1 目录

```
ai/
├── llm/         LLM 接入（API 优先 + 本地双通道）
├── spec/        规约生成 + 求解器反馈闭环
├── rank/        路径优先级排序
├── explain/     反例 → 人话解释
├── patch/       补丁生成 + 有效性验证
├── dataset/     数据获取、清洗、规约样本整理
├── eval/        指标评测脚本（一键出全指标）
└── train/       （条件）QLoRA 微调
```

## 3.2 任务

| # | 任务 | 输出 | **自测方法** | 截止 |
|---|---|---|---|---|
| B1 | 数据集获取（5 个 + 干净负样本） | 本地可用 | 能读出样本 | D2 |
| **B2** | **API baseline** | **"抓到 X%" 的数字** | 数字出来 | **D5** |
| B3 | LLM 接入（API + 本地双通道） | 可切换 | 两边都调通 | D3 |
| B4 | **规约生成 v0** | specs | **能被 A 的 Z3 接受** | D4 |
| B5 | **规约生成闭环** | 迭代后的 specs | 命中率提升 | W2 |
| B6 | 路径优先级排序 | `suspicious_score` | 与随机采样对比 | W2 |
| B7 | 解释生成 | `explanation` | 人读得懂、不空话 | W2 |
| B8 | 补丁生成 + 验证 | `patch.diff` | **打补丁后重跑，漏洞消失** | W2 |
| B9 | **规约样本整理**（Foundry invariant / Certora CVL / Scribble） | 训练集 | ≥ 200 条起步 | W2 |
| B10 | 数据流水线（rejection sampling） | 训练集 | 能夜间自动跑批 | W2 |
| B11 | **评测脚本（一键全指标）** | 指标表 | 一条命令出表 | W3 |
| B12 | （条件）QLoRA + 微调 vs API 对比 | 模型 + 对比表 | baseline 不达标才做 | W3 |
| B13 | 接 A 的 PathSummary | specs | 联调 | W2 |
| B14 | 填 C 的字段（explanation/patch/model_version） | 完整 result | 联调 | W2 |
| B15 | 方案「核心算法说明」+ 测试报告数据部分 | 文档 | — | W4 |

## 3.3 B 的验收标准

> **① 给定合约 + PathSummary，生成能被 Z3 验证的规约；② 一条命令跑出完整指标表（检出/漏报/误报/覆盖率/工具对比）；③ 补丁打上后重跑，漏洞消失。**

## 3.4 B 的止损判据

| rejection sampling 通过率 | 判断 |
|---|---|
| ≥ 20% | ✅ 训有意义 |
| 5-20% | ⚠️ 勉强，需更多候选 |
| **< 5%** | ❌ **放弃训练**，退回"API + 提示工程 + 人工确认"（产品不受影响） |

---

# 第 4 章 人 C 的任务清单（server/ web/ deploy/）

## 4.1 目录

```
server/   api/  queue/  storage/  auth/  obs/
web/      core/（路由/状态/组件库/设计系统）  modules/(M1-M6)  assets/
deploy/   docker/  compose/  proxy/  scripts/
docs/     api/  ops/  user-guide/
```

## 4.2 任务

| # | 任务 | 输出 | **自测方法** | 截止 |
|---|---|---|---|---|
| C1 | **前端基础设施** | 路由 + 状态 + 组件库 + 设计系统 | 空页面能跑 | D2 |
| C2 | 后端 API + 任务队列 | 接口 | **mock 跑通** | D3 |
| **C3** | **M2 审计结果页** ⭐ | 源码高亮 + 证明展开 + 反例轨迹 + 补丁 diff + 重跑 | mock 全流程可点 | D6 |
| C4 | **上传校验 + 速率限制** | 文件类型/大小/内容校验；接口限流 | 传超大/非法文件被拒 | D5 |
| C5 | M1 项目空间 | 项目列表 + **审计 diff** | mock | W2 |
| C6 | M3 规约库 | 列表 + 编辑 + SWC 标注 | mock | W2 |
| C7 | **M6 漏洞知识库** | SWC 分类 + 真实案例 + 复现 + 修复 | mock | W2 |
| C8 | **主流程健壮性** | 超时提示 / 部分成功 / 未覆盖声明 / 空态引导 / 失败重试 | **手动喂各种坏输入** | W3 |
| C9 | M4 报告导出与分享 | PDF（带证明链接） | mock 出报告 | W3 |
| C10 | M5 批量 + CLI + **CI/CD 集成** | 批量任务 + GitHub Action + PR 评论 | 真实仓库试一次 | W3 |
| C11 | **配置管理** | `.env` + 配置校验 + 启动自检 | 缺配置时能明确报错 | W3 |
| C12 | **可观测性** | 日志轮转 + 指标面板 + 健康检查 + 失败告警 | 面板能看到真实数据 | W3 |
| C13 | **部署** | Docker Compose + 反向代理 + HTTPS | **外网能打开** | W3 |
| C14 | **API 文档** | OpenAPI 自动生成 + 示例 | 评审可自助调 | W3 |
| C15 | **完整测试** | 端到端 + 回归测试集 | 一键跑 | W3 |
| C16 | **版本与发布** | 语义化版本 + CHANGELOG + 镜像 tag | 能回滚 | W4 |
| C17 | **依赖与许可审计** | 全部依赖许可清单 | 无 GPL 污染 | W4 |
| C18 | **部署与运维文档 + 用户指南** | docs/ | 外人能照做 | W4 |
| C19 | **演示视频** | MP4，3-5 分钟，≤300MB | 时长严格 | W4 |
| C20 | **材料七件套汇总 + 匿名化自查** | 全套 | 交叉自查 | W4 |

## 4.3 C 的验收标准

> **① 公网可访问，喂 mock JSON 能走完 M1-M6；② 换成真引擎后前端一行不用改；③ 有 API 文档、运维文档、用户指南；④ 上传恶意文件与超量请求均被正确拒绝。**

---

# 第 5 章 集成怎么做

## 5.1 Day 1 上午（三人一起，2 小时）

| 步骤 | 做什么 |
|---|---|
| 1 | 建仓库、三个目录、分支策略（`main` 保护 + 每人一条 feature 分支） |
| 2 | 写 `schemas/path_summary.json` 和 `schemas/analysis_result.json` |
| 3 | **各写一份真实样例**（A 引擎侧 / B AI 侧 / C mock 侧）放 `examples/` |
| 4 | **交叉检查：三份样例能互相读懂 → 冻结** |
| 5 | C 同时办：下载官方模板 + 提交软著申请；B 同时办：开始拉数据集 |

**产出**：`schemas/` 两个文件 + `examples/` 三份样例

## 5.2 Day 3 假集成（半天-1 天）

| 人 | 做什么 |
|---|---|
| A | 用**假数据**生成 `result.json`（跳过真编译） |
| B | 用**假 PathSummary** 生成假 specs |
| C | 用 A/B 的假数据渲染 M1-M6 |

**目的**：**三棵树串起来，暴露 schema 不一致。**
**产出**：接口问题清单 → **当场改 schema（本周唯一允许改的窗口）**

## 5.3 Day 7 真集成（死线）

| 步骤 | 做什么 |
|---|---|
| 1 | 换真组件：真合约 → 真编译 → 真符号执行 → 真规约 → 真 Z3 → 真报告 |
| 2 | 验收：**上传一个合约，出一个带可展开证明的报告** |
| 3 | **三人各自独立跑通一遍**（不是一个人演示） |

**失败排查顺序**：

| 顺序 | 检查 |
|---|---|
| ① | `git log schemas/` —— schema 是否被偷改 |
| ② | 中间产物是否存在（`path_summary.json` 生成了吗） |
| ③ | 工具**原始输出**（别只看封装后的结果） |
| ④ | 切成四段逐段复现 |

## 5.4 每周五集成（1-2 小时）

| 做什么 |
|---|
| 三人各自跑一遍端到端全流程 |
| `git log schemas/` 检查契约 |
| 更新风险清单 |
| **跑一次全量测试 + 看一眼可观测性面板** |

---

# 第 6 章 纪律

## 6.1 禁止清单

| ❌ 禁止 | 后果 |
|---|---|
| 改 `schemas/` 字段名/类型 | 另两人代码当场崩 |
| 在别人目录加文件 | 合并冲突 + AI 上下文污染 |
| 复制别人代码改 | 双份维护 |
| 口头约定接口 | AI 无法据口头约定生成正确代码——**必须落成 schema 文件** |
| 站会超过 15 分钟 | 时间被会议吃掉 |
| 跳过测试直接合并 | 集成时集中爆炸 |

## 6.2 提交纪律

| 规则 |
|---|
| 每次提交必须能通过自己的单元测试 |
| 提交信息格式：`[A|B|C] 模块: 做了什么` |
| **不改别人目录**（CI 会检查） |
| 每日 18:00 前推送到自己的 feature 分支 |

## 6.3 每日节奏

| 时间 | 动作 |
|---|---|
| 09:00-09:15 | 站会：昨日产出 / 今日计划 / **卡点** |
| 09:15-18:00 | 白天：设计、调试、集成、实验判断 |
| **18:00** | **提交夜间批处理任务** |
| 次日 09:00 | 看批处理结果 |

**夜间跑批池**：合约批量分析（A）· rejection sampling（B）· QLoRA 训练（B）· 全量指标评测（B）· 回归测试（三人）· 数据集预处理（B）

---

# 第 7 章 排期（28 天）

| 周 | A | B | C |
|---|---|---|---|
| **W1 D1** | 三人：冻结 schema + 建仓库 | | C：下载模板 + 软著 |
| **W1 D2-6** | 编译多版本 → **沙箱** → 符号执行 → Z3 → 反例解析 | 数据集 → **baseline** → LLM → 规约 v0 | 前端基础设施 → 后端 → **M2** → 上传校验 |
| **🔴 W1 D7** | **真集成死线：上传合约 → 带可展开证明的报告** | | |
| **W2** | 路径裁剪 + 覆盖率 + 可观测性 + 接 B/C | 规约闭环 + 排序 + 解释 + 补丁 + 规约样本 + 流水线 | M1 + M3 + M6 + 健壮性 |
| **W2 D14** | **中期集成 + 第一次完整指标** | | |
| **W3** | 测试 + 并发性能 | **完整指标 + 微调对比** | M4 + M5/CI + 配置 + 可观测 + **上线** + API 文档 + E2E + 招募用户 |
| **W4** | 部署文档 + 代码章节 | 数据成表 + 算法章节 | 版本发布 + 许可审计 + 运维/用户文档 + **视频** + 材料 + **匿名化自查** |
| **W4 D28** | **代码冻结 + 材料冻结** | | |

---

# 第 8 章 Day 1 上午的 6 件事

| # | 事项 | 谁 |
|---|---|---|
| 1 | 建仓库 + 三个目录 + 分支策略 | 三人 |
| 2 | 写两个 schema | 三人 |
| 3 | 各写样例并交叉检查 → 冻结 | 三人 |
| 4 | **下载官方作品方案模板**（网盘 pwd=2026） | C |
| 5 | **提交软著申请** | C |
| 6 | 开始拉数据集（含干净合约负样本） | B |
