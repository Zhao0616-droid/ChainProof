# deploy/ — 部署(人 C)

> 目标:Docker Compose 一键起、公网 HTTPS 可访问、可回滚(分工手册 C13 / 品质清单 D1-D7)。

## 目录

| 目录 | 内容 |
|---|---|
| docker/ | Dockerfile 与镜像构建(引擎沙箱镜像 / API 镜像 / 前端镜像) |
| compose/ | docker-compose.yml 编排 |
| proxy/ | 反向代理配置(HTTPS) |
| scripts/ | 部署/备份/回滚脚本 |

占位 — 由人 C 在 W3 实现。
